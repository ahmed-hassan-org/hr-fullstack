export enum WapelProviders {
  /** used to inject current app name */
  APP_NAME_PROVIDER = 'appName',
  /** used to inject local storage to use it in the global or library */
  APP_LOCALSTORAGE_PROVIDER = 'appLocalStorage',
}
